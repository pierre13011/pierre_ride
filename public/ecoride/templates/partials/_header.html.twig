<header>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <img src="{{ asset('img/eco.png') }}" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
                EcoRide
            </a>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_covoiturage') }}">Covoiturage</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                </ul>
            </div>

            <div class="d-flex justify-content-center gap-3">
                {% if app.user %}
                    <a href="{{ path('app_profile') }}" class="btn btn-light">Profil</a>
                    <a href="{{ path('app_logout') }}" class="btn btn-dark">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-light">Connexion</a>
                    <a href="{{ path('app_inscription') }}" class="btn btn-dark">Inscription</a>
                {% endif %}
            </div>
        </div>
    </nav>
</header>
